<a href="#" class="<%= category.product_id? ? "product" : "category"  %>" id="category_<%= category.id %>" <%= "mdata=\"{product_id: #{category.product_id}}\"" if category.product_id?%>><%= category.product_id? ? category.product.name : category.name %></a>